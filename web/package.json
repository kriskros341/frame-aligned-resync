{
  "name": "frame-aligned-resync-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser port of frame-aligned-resync — runs fully client-side.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "verify": "node verify-core.mjs",
    "verify:audio": "node verify-audio.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "mp4-muxer": "^5.2.2",
    "mp4box": "^2.3.0",
    "pyodide": "^0.29.4"
  }
}
